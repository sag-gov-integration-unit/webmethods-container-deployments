# Common AWS ECS Infrastructure Cloudformation Template

Deploy:

```
aws cloudformation deploy --template ./main.yaml --stack-name webmethods-samples-aws
```


```
aws cloudformation delete-stack --stack-name webmethods-samples-aws
```